<script lang="ts">
  type Props = {
    eyebrow?: string;
    title?: string;
    lead?: string;
    align?: 'left' | 'center';
  };

  let {
    eyebrow = '',
    title = '',
    lead = '',
    align = 'left'
  }: Props = $props();
</script>

<div class={`mx-auto max-w-3xl ${align === 'center' ? 'text-center' : ''}`}>
  {#if eyebrow}
    <p class="mb-3 text-[11px] font-semibold uppercase tracking-[0.24em] text-white/56 sm:text-xs">
      {eyebrow}
    </p>
  {/if}

  <h2 class="m-0 text-balance text-[clamp(2rem,4vw,3.5rem)] font-black leading-[0.98] tracking-[-0.055em] text-white">
    {title}
  </h2>

  {#if lead}
    <p class="mt-4 text-pretty text-base leading-7 text-white/70 sm:text-lg">
      {lead}
    </p>
  {/if}
</div>
