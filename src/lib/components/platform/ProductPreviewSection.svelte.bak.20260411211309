<script lang="ts">
  import SectionHeading from '$lib/components/ui/SectionHeading.svelte';

  type Stat = { label: string; value: string };
  type Item = { label: string; title: string; body: string; stats?: Stat[] };
  type Props = {
    id?: string;
    eyebrow?: string;
    title?: string;
    lead?: string;
    items?: Item[];
  };

  let {
    id = 'product-preview',
    eyebrow = '',
    title = '',
    lead = '',
    items = []
  }: Props = $props();
</script>

<section {id} class="px-4 py-12 sm:px-6 lg:px-8 lg:py-16">
  <div class="mx-auto max-w-7xl">
    <SectionHeading {eyebrow} {title} {lead} />
    <div class="mt-8 grid gap-4 xl:grid-cols-3">
      {#each items as item}
        <article class="rounded-[30px] border border-white/10 bg-white/[0.03] p-5 backdrop-blur">
          <p class="m-0 text-xs font-semibold uppercase tracking-[0.22em] text-white/48">{item.label}</p>
          <h3 class="mt-3 mb-0 text-2xl font-extrabold tracking-[-0.03em] text-white">{item.title}</h3>
          <p class="mt-3 text-sm leading-7 text-white/72">{item.body}</p>

          {#if item.stats?.length}
            <div class="mt-5 grid gap-3 sm:grid-cols-3 xl:grid-cols-1">
              {#each item.stats as stat}
                <div class="rounded-2xl border border-white/8 bg-black/10 px-4 py-3">
                  <p class="m-0 text-[11px] font-semibold uppercase tracking-[0.18em] text-white/45">{stat.label}</p>
                  <p class="mt-2 mb-0 text-lg font-bold tracking-[-0.03em] text-white">{stat.value}</p>
                </div>
              {/each}
            </div>
          {/if}
        </article>
      {/each}
    </div>
  </div>
</section>
