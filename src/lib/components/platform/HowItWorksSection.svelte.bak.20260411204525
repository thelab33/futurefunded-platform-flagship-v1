<script lang="ts">
  import SectionHeading from '$lib/components/ui/SectionHeading.svelte';

  type Step = { step: string; title: string; body: string };
  type Props = {
    id?: string;
    eyebrow?: string;
    title?: string;
    lead?: string;
    steps?: Step[];
  };

  let {
    id = 'how-it-works',
    eyebrow = '',
    title = '',
    lead = '',
    steps = []
  }: Props = $props();
</script>

<section {id} class="px-4 py-12 sm:px-6 lg:px-8 lg:py-16">
  <div class="mx-auto max-w-7xl">
    <SectionHeading {eyebrow} {title} {lead} />
    <div class="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-4">
      {#each steps as item}
        <article class="rounded-[28px] border border-white/10 bg-white/[0.03] p-5 backdrop-blur">
          <p class="m-0 text-xs font-semibold uppercase tracking-[0.22em] text-white/48">{item.step}</p>
          <h3 class="mt-3 mb-0 text-xl font-extrabold tracking-[-0.03em] text-white">{item.title}</h3>
          <p class="mt-3 text-sm leading-7 text-white/70">{item.body}</p>
        </article>
      {/each}
    </div>
  </div>
</section>
